// Code wars Kata8 Solutions

// Beginner - Lost Without a Map

// Given an array of integers, return a new array with each value doubled.

function maps(x) {
  const doubledArray = x.map((y) => y * 2);

  return doubledArray;
}

console.log(maps([1, 2, 3, 4]));
